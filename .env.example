# ====================================
# K9 Operations Management System
# Environment Variables Configuration
# ====================================

# ==== REQUIRED FOR PRODUCTION ====

# Flask Environment (development/production)
FLASK_ENV=production

# Session Secret Key (REQUIRED)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SESSION_SECRET=your_secure_session_secret_here

# PostgreSQL Database Connection (REQUIRED)
# Format: postgresql://username:password@host:port/database_name
DATABASE_URL=postgresql://k9user:your_password@localhost:5432/k9_operations

# Individual PostgreSQL Variables (Optional - if not using DATABASE_URL)
PGHOST=localhost
PGPORT=5432
PGDATABASE=k9_operations
PGUSER=k9user
PGPASSWORD=your_password

# ==== WEB SERVER CONFIGURATION ====

# Gunicorn Workers (recommended: 2-4 per CPU core)
GUNICORN_WORKERS=4

# ==== OPTIONAL FEATURES ====

# Email Configuration (for password reset, notifications)
# Using SendGrid:
SENDGRID_API_KEY=your_sendgrid_api_key
MAIL_DEFAULT_SENDER=noreply@yourdomain.com

# OR using SMTP:
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_password

# Google Drive Backup (Optional)
GOOGLE_DRIVE_CREDENTIALS={"type":"service_account",...}
GOOGLE_DRIVE_FOLDER_ID=your_folder_id

# Dropbox Backup (Optional)
DROPBOX_ACCESS_TOKEN=your_dropbox_token
DROPBOX_REFRESH_TOKEN=your_refresh_token
DROPBOX_APP_KEY=your_app_key
DROPBOX_APP_SECRET=your_app_secret

# ==== SECURITY NOTES ====
# 1. Change PGPASSWORD to a strong, unique password
# 2. Generate a secure SESSION_SECRET using cryptographically secure methods
# 3. IMPORTANT: Change default admin credentials immediately after first login!
# 4. For production deployment:
#    - Use a firewall to restrict database access
#    - Enable SSL/TLS encryption with Let's Encrypt
#    - Set up regular database backups
#    - Monitor logs for security issues
# 5. Consider using a key management service for sensitive values

# ==== PRODUCTION DEPLOYMENT CHECKLIST ====
# [ ] Changed PostgreSQL password from default
# [ ] Generated secure SESSION_SECRET
# [ ] Changed default admin credentials
# [ ] Configured Nginx reverse proxy with SSL
# [ ] Set up database backups (automated)
# [ ] Configured log rotation
# [ ] Restricted network access to database
# [ ] Tested all critical workflows
